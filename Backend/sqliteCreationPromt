CREATE TABLE IF NOT EXISTS sticker (
    id          INTEGER PRIMARY KEY,
    name        TEXT NOT NULL,
    description TEXT,
    lat         REAL NOT NULL,
    long        REAL NOT NULL,
    imageLink   TEXT 
);
